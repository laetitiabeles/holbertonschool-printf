#include "main.h"

/**
* printPercent - function that prints percent
* @arguments: list of arguments
* Return: the length
*/

int printPercent(va_list arguments)
{
	(void) arguments;

	_putchar('%');
	return (1);
}
